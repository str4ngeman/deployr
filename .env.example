# Server port (default: 4199)
PORT=4199

# Root directory for file explorer (default: /opt)
FILE_ROOT=/opt

# Enable Docker container/log access (requires docker.sock mount)
DOCKER_ENABLED=false

# SQLite data directory
DATA_DIR=./data
